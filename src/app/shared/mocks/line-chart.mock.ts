import { LineChart } from '../models/line-chart.model';

const lineChartData: LineChart = {
  labels: ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho'],
  values: [990, 1249, 1590, 3403, 5000, 7000],
};

export const lineChartNull = {
  labels: ['0', '0', '0', '0', '0', '0'],
  values: [0, 0, 0, 0, 0, 0],
};

export default lineChartData;
